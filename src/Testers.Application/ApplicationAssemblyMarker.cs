namespace Testers.Application;

// Anchor for Scrutor / FluentValidation / EndpointScanner assembly scans, since IDispatcher
// (the marker we used before) now lives in Testers.SharedKernel.
public sealed class ApplicationAssemblyMarker;
